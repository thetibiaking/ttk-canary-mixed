-- local vip = Action()

-- function vip.onUse(player, item, fromPosition, target, toPosition, isHotkey)
	-- VipSystemOldRequireChanges
    -- if not player:isVip() then
        -- player:getStorageValue(Storage.BoxMago, 1)
        -- player:sendCancelMessage('Você precisa ser vip para abrir esta caixa')
    -- else
        -- player:setStorageValue(Storage.VipAccess, 1)
        -- player:setStorageValue(Storage.BoxMago, 3)
        -- player:sendTextMessage(MESSAGE_EVENT_ADVANCE, "Pronto, voce computou que faz parte dos players vip's. Obrigado!")
    -- end
    -- return false
-- end

-- vip:aid(61998)
-- vip:register()