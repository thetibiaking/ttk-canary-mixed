-- local config = {
    -- [60121] = Position(32250, 32191, 12)
-- }

-- local salaEventos = MoveEvent()

-- function salaEventos.onStepIn(creature, item, position, fromPosition)
    -- local player = creature:getPlayer()
    
    -- if not player then
        -- return true
    -- end
    
    -- local targetPosition = config[item.uid]
    -- if not targetPosition then
        -- return true
    -- end
    
    -- player:teleportTo(Position(1172, 1023, 7))
    -- player:sendTextMessage(MESSAGE_EVENT_ADVANCE, "Avalon")
-- end

-- salaEventos:type("stepin")

-- for index, value in pairs(config) do
    -- salaEventos:uid(index)
-- end

-- salaEventos:register()
